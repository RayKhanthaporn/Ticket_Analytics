Install dependencies
Run this once in Command Prompt, PowerShell, Terminal or a notebook cell:

python -m pip install --upgrade pandas requests openpyxl
pip install --upgrade pandas requests openpyxl xlrd

Optional, if running in Jupyter:

python -m pip install --upgrade jupyter